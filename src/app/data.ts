export interface WordsImg {
  title: string,
  subtitle: string,
  imgUrl: string,
}

export interface WorkCard {
  title: string;
  description: string;
  imgUrl: string;
  color: string;
  link: string;
  hoverText: string;
}
